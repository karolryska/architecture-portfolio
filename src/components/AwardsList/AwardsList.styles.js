import styled from 'styled-components';

export const Wrapper = styled.ul`
    width: 30vw;
    list-style: none;
`;

export const Award = styled.p`
    margin-bottom: 16px;
    font-size: 22px;
    font-weight: 400;
    cursor: default;

    &:hover {
        color: orange;
    }
`;
